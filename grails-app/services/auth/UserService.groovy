package auth

import grails.transaction.Transactional

@Transactional
class UserService {

    def save(Map postData) {
        Map resultmap = [:]

        return resultmap
    }

    def update(Map postData) {
        Map resultmap = [:]

        return resultmap
    }

    def show(Long id) {
        Map resultmap = [:]

        return resultmap
    }

    def search(Map postData) {
        Map resultmap = [:]

        return resultmap
    }
}
